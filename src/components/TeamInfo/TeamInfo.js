import React from 'react';
import classes from './TeamInfo.module.css';

export default () => {
  return (
    <div className={classes.TeamInfo}>
      Это блок с информацией о команде
    </div>
  );
}